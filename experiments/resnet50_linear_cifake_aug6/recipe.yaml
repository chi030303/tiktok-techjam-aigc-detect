# 2026-08-29, tianqi, CIFAKE train: clean + all 6 official ops per image (online, no disk dump)
name: resnet50_linear_cifake_aug6
description: Full CIFAKE, frozen ResNet-50, each train image seen as clean + 6 official ops.
owner: kiki

backbone: resnet-50
head: linear
freeze_backbone: true

train:
  datasets:
    - cifake
  forbid:
    - val
    - evalgen
    - demo_wildfake

eval:
  - cifake_test

aug:
  online: true
  expand: six_ops
  jpeg: true
  blur: true
  resize: true
  noise: true
  jitter: true
  center_crop: true

optim:
  epochs: 3
  batch_size: 128
  lr: 0.001
  num_workers: 8
  cache_features: false

gpu: "1"
# end
